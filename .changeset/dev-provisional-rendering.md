---
'astro-image-processor': minor
---

Add non-blocking dev rendering with provisional placeholders and background compression. Introduces `devConcurrency`, `devPlaceholder`, and `devReloadOnCompressComplete` integration settings.
